# Local development:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# Docker: handled by Dockerfile (no venv needed)

huawei-lte-api~=1.6.9
python-dotenv~=1.0.0
